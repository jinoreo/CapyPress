location /capypress {
    alias /path/to/your/app;
    try_files $uri $uri/ /capypress/index.html;

    # 添加以下配置来处理静态资源
    location ~ \.(css|js|svg|png|jpg|jpeg|gif)$ {
        try_files $uri =404;
    }
} 